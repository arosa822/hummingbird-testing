# Phase 1 Test: curl image
# Purpose: Verify Hummingbird curl image can be used as a base for simple HTTP operations

FROM quay.io/hummingbird/curl:latest

# The image already has curl installed and configured
# This Dockerfile demonstrates using it as a base image
# Entrypoint is already set to /usr/bin/curl
