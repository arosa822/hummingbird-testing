# Phase 1 Test: jq image
# Purpose: Verify Hummingbird jq image can process JSON data

FROM quay.io/hummingbird/jq:latest

# The image already has jq installed and configured
# This Dockerfile demonstrates using it as a base image
# Entrypoint is already set to /usr/bin/jq
